1. Create .env file, with content from .env.example file and fill with your Deepl API KEY
2. Copy files to be translated to `/input` directory
3. Switch node version to 16.11.0
4. Run `npm run translate -- <2 letter symbol of language of your choice>`
5. When script finishes, your translations will be available in `/output` folder
